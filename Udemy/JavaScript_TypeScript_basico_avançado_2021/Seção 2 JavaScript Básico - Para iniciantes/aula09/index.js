// String, number, underfined, null, boolean, symbol
const nome = 'Rodrigo' // String
const nome1 = "Rodrigo" // String
const nome2 = `Rodrigo` // String
const num1 = 10 // Number
const num2 = 10.52 // Number
let nomeAluno // underfined -> não aponta pra local nenhum na memória
const sobreNomeAluno = null // Nulo -> não aponta pra local nenhum na memória
const aprovado = true // Boolean = true ou false (lógico)

console.log(typeof(sobreNomeAluno), sobreNomeAluno);